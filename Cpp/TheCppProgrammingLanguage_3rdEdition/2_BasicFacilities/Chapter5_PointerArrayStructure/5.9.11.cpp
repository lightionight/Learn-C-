/**
 * 5.9.11. P105
 * Read a sequence of words from input.
 * Use Quit as aword that terminates the input.
 * Print the words in the order they were enterd.
 * Don't Print word Twice.
 * Modifity the program sort the words before Print them.
*/
/******************************************************
 * AUTHOR: FENG
 * TIME：2020.10.28
 * ***************************************************/

#include "cppBaseInclude.h"
#include <string>
#include <array>
void getInput(std::string& test);

int main(){
    std::string data;
    getInput(data);

}




void getInput(std::string& test)
{
    std::cin >> test;
    
}

void sortString(std::string& input)
{
    for(int i = 0; i < input.length(); i++)
    {
        if((input[i]))
    }
}