/***************************************************
  About Character input and output Test 
  #include head flies
  <stdio.h>   include getchar(), putchar() and EOF
  EOF is sepcila variable  mean "End Of Files"
  **************************************************/

#include <stdio.h>

int main(){
    int c, bool;

    //while((c = getchar()) != EOF);{
    //    putchar(c);
    //}      
    if(EOF)
        printf("This is EOF value : 1");
    else
        printf("This is EOF value : 0");
    size
    return 0;
}

int main(){
    int c, bool;

    //while((c = getchar()) != EOF);{
    //    putchar(c);
    //}      
    if(EOF)
        printf("This is EOF value : 1");
    else
        printf("This is EOF value : 0");
    
    return 0;
}